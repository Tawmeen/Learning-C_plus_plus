#include<iostream>
using namespace std;
int main(){
    //integer(whole number)

   int days=7;
   int age=20;
   int year=2024;

   //double(number including decimal)

     double temp=35.6;
     double gpa=4.83;
     double price=2.99;

     //character(store only single character)

     //when you diclar character variable,you have to use ('') in stored value.

     char grade='A';
     char initail='C';
     char dollarsing='$';

     //boolean(true or false)

     bool student=true;
     bool power=false;
     bool for_sale=true;
     
     //string(objects that represents a sequence of text)

     //string variable is kind of same like character variable but main 2 differene is,
     //1.you can store more than one character.
     //you have to use(" ") in stored value.

    string name="tawmeen hasan al hasib";
    string day="Friday";
    string food="pizza";
    string adreess="16/5 galachipa";
    cout<<adreess;
     


    return 0;
}